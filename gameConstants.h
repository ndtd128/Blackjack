#pragma once

//Window
const int SCREEN_WIDTH = 1366;
const int SCREEN_HEIGHT = 768;
const char WINDOW_TITLE[] = "Blackjack";
const int DELAY_TIME = 1000;

//Game starter pack
const int PLAYER_START_CASH = 100;
const int BLACK_JACK = 21;

const int FULL_DECK_SIZE = 52;
const int MAX_PLAYER_CARDS = 12;
const int MAX_DEALER_CARDS = 10;

const int CARD_WIDTH = 120;
const int CARD_HEIGHT = CARD_WIDTH * 3 / 2;

const int CARD_XSPACE = CARD_WIDTH / 5 * 2;
const int CARD_YSPACE = CARD_HEIGHT / 2;

const int RES_WIDTH = SCREEN_WIDTH / 3;
const int RES_HEIGHT = RES_WIDTH / 2;